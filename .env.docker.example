# Copy to .env.docker (gitignored) before building the production web image.
FRONT_DIST_PATH=dist/app-taller-mecanico/browser
REGISTRY_HOST=registry.example.com
WEB_IMAGE=mechanics/web
# WEB_VERSION=
